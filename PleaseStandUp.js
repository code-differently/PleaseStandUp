class PleaseStandUp {

    /*
    Have the function howOld(birthday) take the birthday parameter being passed and 
    return the age of a person with the date as their birthday.
    (ie. if birthday = June 22, 1996 then the output should be 24)
    */

   howOld(birthday){
        return -1;
    }

    howOld(birthday)
    // calculate month
    let month =       // 12 months in a year
    // calculate day
    let day =           // 365 days in a year
    // calculate year
    let year =      // 4 numerical values/positions
    // results / output of month, day, year / put into an array
    let results = $["month", "day", "year"] 
    // output results
    return results


    /*
    Have the function timeConvert(numOfMins) take the numOfMins parameter being passed and 
    return the number of hours, minutes, and seconds the parameter converts to 
    (ie. if num = 63.5 then the output should be 1:3:30). Separate the number of hours,
    minutes, and seconds with colons.
    */

    timeConvert(numOfMins){
        return -1;
    }

}

timeConvert(numOfMins)
// take the num value and then divide it by 60 min to give the hours in the num value

// if there is a remainder from the hour value, then the remainder of num will populate the minute value

// if there is a remainder from the minute value, then remainder will populate the seconds value

// put all the values in an array and add ":" to separate the hour, minute and second values.

// results / output of the array



module.exports = PleaseStandUp;